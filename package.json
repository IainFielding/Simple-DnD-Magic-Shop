{
  "name": "sogrom-simple-dnd5e-magic-shop",
  "private": true,
  "type": "module",
  "description": "Dev tooling (lint, unit tests, release packaging) for the Simple D&D Magic Shop Foundry module. Not shipped in the module archive.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "validate:syntax": "node tools/validate-syntax.mjs",
    "validate:json": "node tools/validate-json.mjs",
    "validate:lang": "node tools/validate-lang.mjs",
    "validate:package": "node tools/validate-package.mjs",
    "package": "node tools/build-release.mjs",
    "check": "npm run validate:syntax && npm run validate:json && npm run validate:lang && npm run validate:package && npm run lint && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.14.0",
    "eslint": "^9.14.0",
    "globals": "^15.12.0",
    "vitest": "^2.1.4"
  }
}
